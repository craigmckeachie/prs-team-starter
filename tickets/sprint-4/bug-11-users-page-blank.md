# BUG-11 — The Users page won't load

**Reported by:** Priya, IT admin

The Users page stopped working. It worked last week.

**Steps to reproduce**

1. Sign in
2. Click **Users** in the sidebar

**Expected:** the user card grid.
**Actual:** instead of the list I get a page saying *"Oops! Sorry, an unexpected error has
occurred."* The navigation bar disappears along with it, so I have to use the browser's Back
button to get anywhere. The Products and Vendors pages are fine. The API returns users
correctly when I check it in Insomnia — it's only the page.
